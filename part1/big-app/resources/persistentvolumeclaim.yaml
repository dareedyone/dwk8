apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: "bigapp-namespace"
  name: shared-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
